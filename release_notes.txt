CRITICAL FIX: All database operations now work!

Root cause: schedules, peer_rooms, pomodoro_sessions, notes, habits, heatmap_entries, user_topic_progress, user_badges, user_milestones, test_submissions, progress_cards, leaderboard_entries, youtube_links ALL had foreign key constraints pointing to the custom 'users' table instead of 'auth.users'. Since Supabase Auth creates users in 'auth.users', every INSERT failed silently.

Fix: Dropped and recreated all FKs to reference auth.users(id).

Also: Seeded exam/subject/module/chapter/topic data for all 9 exams (CA, CS, CMA, CFA, JEE, NEET).
