v3.1.5 - Critical Bug Fixes + More Tests

BUG FIXES:

Notes:
- Fixed notes not saving (DB had NOT NULL constraint on chapter_id)
- chapterId now nullable in model and DB
- Added error handling with snackbar feedback on save failures

Habits:
- Added error handling for habit creation and check-in
- Failed operations now show red error snackbar

Home Screen:
- Fixed all stats going blank when any single API call fails
- Each stat loads independently - one failure no longer kills everything

Flashcards:
- Improved back-side content with more useful context per topic

MORE CONTENT:
- Added 17 new mock tests across all 9 exams (CA, CS, CMA, CFA, JEE, NEET)
- 85 new questions with explanations
- Each exam now has 4-5 tests available
