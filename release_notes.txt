Major fixes:

- Back Navigation: Added back buttons to all non-shell screens, changed go() to push() for detail screens so system back works
- RLS Policies: Applied Row Level Security for ALL tables: schedules, schedule_items, peer_rooms, peer_room_members, pomodoro_sessions, notes, habits, habit_entries, user_topic_progress, user_badges, heatmap_entries, tests, badges, exams, subjects, chapters, topics
- Schedule: Can now create schedules and add tasks
- Peer Rooms: Can now create and join rooms
- Navigation: Detail screens use push() so back button works properly
